# Install as /etc/fireball-camera-pull.conf with owner root:root and mode 0600.
# Generate CMS_TOKEN once with: openssl rand -hex 32
CMS_ENDPOINT=https://maxipapa.ru/api/camera-manager/pull
CMS_TOKEN=PASTE_THE_64_CHARACTER_HEXADECIMAL_TOKEN_HERE
